version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

sphinx:
  configuration: docs/conf.py

# Tell Read the Docs to install your package and its dependencies before running Sphinx
python:
  install:
    - method: pip
      path: .
